name: Cricbites
description: A cricket app with login and splash screen functionality
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get:
#  go_router:
  flutter_screenutil:
  pinput:
  lottie:
  image_picker:
  permission_handler:
  carousel_slider:
#  reels_viewer:
  dio:
  intl:
  shared_preferences:
  share_plus:
  webview_flutter:
  in_app_review:
  youtube_player_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/